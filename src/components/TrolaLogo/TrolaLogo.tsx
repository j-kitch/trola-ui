import { FC } from "react";
import classes from "./TrolaLogo.module.css";

const TrolaLogo: FC = () => {
    return (
        <div className={classes.logo}>
            Tr
        </div>
    );
};

export default TrolaLogo;